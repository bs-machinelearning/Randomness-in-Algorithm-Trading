# baseline.py — forwarder / placeholder for baseline policy (P2 owns)
def generate_trades(prices: "pd.DataFrame") -> "pd.DataFrame":
    """
    Runner entry point for the baseline policy (owned by P2).
    Replace this with real baseline implementation or call into a class.
    Expected columns: ['date','symbol','side','qty','ref_price'].
    """
    raise NotImplementedError("Baseline policy: implement generate_trades(prices).")
